Group Notify

Optionally send an email to Group Members when new Group Content is created.

To enable email notifications:

Browse to Administration -> Groups -> Group types -> Set available content.
Plugins provided by "Group Node" (gnode) can be enabled to send email notifications.
Click "Install" or "Configure" and check "Notify group members" to enable.
